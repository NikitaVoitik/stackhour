[package]
name = "stackhour-bench-iced"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
benchmark-core = { path = "../crates/benchmark-core" }
iced = { version = "=0.14.0", features = ["canvas", "tokio"] }
regex = "1"
serde_json = "1"

[workspace]

[profile.release]
lto = "thin"
strip = true
codegen-units = 1
