[package]
name = "stackhour-bench-egui"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
benchmark-core = { path = "../crates/benchmark-core" }
eframe = { version = "=0.35.0", default-features = false, features = ["default_fonts", "glow", "wayland", "x11"] }
regex = "1"
serde_json = "1"

[workspace]

[profile.release]
lto = "thin"
strip = true
codegen-units = 1
