[package]
name = "stackhour-bench-gpui"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
benchmark-core = { path = "../crates/benchmark-core" }
gpui = { version = "=0.2.2", default-features = false, features = ["wayland", "x11"] }
regex = "1"
serde_json = "1"

[workspace]

[profile.release]
lto = "thin"
strip = true
codegen-units = 1
