{
  "description": "Enforce Stackhour verification during Codex sessions.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "\"$(git rev-parse --show-toplevel)\"/dev/agent-verify-hook session-start",
            "timeout": 10,
            "statusMessage": "Loading repository verification policy"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|apply_patch",
        "hooks": [
          {
            "type": "command",
            "command": "\"$(git rev-parse --show-toplevel)\"/dev/agent-verify-hook post-edit",
            "timeout": 600,
            "statusMessage": "Running fast repository verification"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"$(git rev-parse --show-toplevel)\"/dev/agent-verify-hook stop",
            "timeout": 3600,
            "statusMessage": "Running required repository verification"
          }
        ]
      }
    ]
  }
}
